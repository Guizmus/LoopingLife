UI.componentClasses.infobox = {
   selector : '.needInfobox',
   html : function (index) { // index is the index of the element in the selected list. Used if the selector is multiple
     var html = "";
     return html;
   },
   eventListeners : [
   ],
};
